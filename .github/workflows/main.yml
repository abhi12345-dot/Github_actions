name: ORT scan for tiny-express

on:
  workflow_dispatch:

jobs:
  ort-analysis-and-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Clone source_projects repository
        run: |
          git clone https://github.com/OSS-PRO/source_projects.git
          ls source_projects/tiny-express  # Verify the directory exists

      - name: Create ORT directories
        run: |
          mkdir -p ort-output ort-config
          sudo chmod -R 777 ort-output

      - name: Write ORT config
        run: |
          cat <<EOF > ort-config/config.yml
          ort:
            analyzer:
              allowDynamicVersions: true
            scanner:
              ignorePatterns:
                - "**/node_modules/**"
          EOF

      - name: Run ORT analyze
        run: |
          docker run --rm \
            -u root \
            -e ORT_CONFIG_DIR=/ort/config \
            -v ${{ github.workspace }}/source_projects/tiny-express:/project \
            -v ${{ github.workspace }}/ort-output:/ort/output \
            -v ${{ github.workspace }}/ort-config:/ort/config \
            ghcr.io/oss-review-toolkit/ort:latest \
            analyze -i /project -o /ort/output

      - name: Upload ORT results
        uses: actions/upload-artifact@v4
        with:
          name: ort-results
          path: ort-output/
